{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "viewer",
  "description": "Capabilities for the shared paper viewer window",
  "windows": ["arxiv-viewer"],
  "permissions": [
    "core:default"
  ]
}
